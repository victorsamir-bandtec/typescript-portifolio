import { extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
  fonts: {
    body: "Inter,system-ui, sans-serif",
    heading: "Inter, system-ui, Georgia, serif",
    mono: "Menlo, monospace",
  },
})

export default theme;


